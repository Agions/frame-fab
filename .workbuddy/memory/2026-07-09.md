# 2026-07-09

## 设计侧任务（frame-fab / Story Weaver）

- 用户要求：重构/升级 UI 设计系统、重做关键界面、出组件库、明/暗主题与一致性体检。
- 已加载 ardot-design-core + ardot-ui-design 两个 skill（画布设计工作流）。
- **体检已完成**（基于源码）：`docs/design-audit.md`。核心问题：
  - 双全局样式冲突：`src/app/styles/index.css`(硬编码 hsl/#f5f5f5) 与 `src/styles/globals.css`(token 源) 并存。
  - `components.json` 指向不存在的 `tailwind.config.js`（实为 .ts）。
  - 令牌命名两套并存（自定义 `--color-*` + shadcn `--*`）；间距/圆角/阴影刻度偏多；muted-foreground 浅色对比度临界 AA；字体系统缺失（无 `--font-sans`/字阶/CJK 栈）；暗色三选择器可收敛为 `.dark`。
- **阻塞**：Ardot 画布适配器三次重试仍 NO_ADAPTER（环境未连接 Ardot 服务）。按 skill 规则停止画布渲染，未走替代路径。待用户连通适配器后续做画布设计稿；或改本地 HTML 交付（需用户确认）。
- **用户决策**：改用本地 HTML 交付（用户明确选择，非替代路径规避）。已生成 `docs/design-system.html`（自包含单文件）：设计令牌板（品牌/语义/中性色阶 + 字阶/间距/圆角）+ 组件库（Button/Input/Select/Switch/Checkbox/Badge/Tabs/Avatar/Progress/Alert/Toast/Card/Dialog）+ 创作工作台关键界面重做（侧栏+顶栏+模式选择+创作区+七步流水线）+ 明/暗切换（.dark 单入口 + localStorage 记忆）。按体检结论落地单一令牌源/收敛刻度/AA 对比度/CJK 字体栈。
- **12:15 重试**：用户说"继续"希望上画布，再次加载 ardot-design-core + ardot-design-router，create_design 触发异步加载后 fetch_file_info 再三次重试仍 NO_ADAPTER。按 skill 硬规则停止画布任务，未走替代路径。结论：Ardot 适配器环境仍不可达；画布设计稿需用户先真正连通 Ardot 服务（打开 Ardot 应用 + 活动文件 + 信任连接器）后才能落地。已有 HTML 交付为可用产物。
