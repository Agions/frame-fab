/**
 * Shim: backward compat for `@/core/types`.
 * Real location: `@/core/types/index.ts` (source of truth for legacy types).
 * Use `@/shared/types` for new code.
 */
export * from '@/core/types/index';
